services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cdlab_chat
      POSTGRES_USER: cdlab
      POSTGRES_PASSWORD: cdlab
    volumes:
      - cdlab_chat_postgres_data:/var/lib/postgresql/data

volumes:
  cdlab_chat_postgres_data:
